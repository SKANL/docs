# Tasks: universal-doc-harness

## Review Workload Forecast

| Field | Value |
|-------|-------|
| Estimated changed lines | ~2,250 total across 8 slices (~200-340 each) |
| 400-line budget risk | High (single PR) / Low-Medium (per slice) |
| Chained PRs recommended | Yes |
| Suggested split | PR1 -> PR2 -> PR3 -> PR4 -> PR5 -> PR6 -> PR7 -> PR8 |
| Delivery strategy | auto-chain |
| Chain strategy | stacked-to-main |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: Low-Medium (per slice); High if collapsed to one PR

### Suggested Work Units (all target `main`, merge in order, main stays green after each)

| Unit | Goal | PR | Est. lines | Depends on |
|------|------|----|-----------:|------------|
| 1 | Sentinel rename + asset-kind generalization + quick debt (deps, except-fix) | PR1 | ~220 | none |
| 2 | Repository port segregation (ISP) | PR2 | ~260 | PR1 |
| 3 | CLI composition root split | PR3 | ~320 | PR2 |
| 4 | Renderer registry + data-driven stage plan | PR4 | ~300 | PR3 |
| 5 | opendataloader-pdf verification + ingest detect/route infra | PR5 | ~260 | PR4 |
| 6 | Ingest per-type adapters (pandoc, PDF, md/txt) | PR6 | ~340 | PR5 |
| 7 | Context files + progressive-disclosure index | PR7 | ~300 | PR6 |
| 8 | Pipeline wiring + app-layer test/debt closeout | PR8 | ~250 | PR7 |

Strict TDD is active (`uv run pytest`): every implementation task is preceded by its RED (failing test) task.

---

## PR1 — Sentinel Rename + Asset Generalization (Foundation)
Base: `main`. Covers: document-pipeline (`No hardcoded format identifiers remain`, deps/error-handling reqs), asset-management (both requirements).

- [x] 1.1 RED: add/adjust tests in `tests/unit/domain/test_evidence.py`, `test_collection.py`, `test_rules.py`, `test_sections.py`, `test_review.py`, `test_section_rendering.py` asserting no `"tesina"` literal remains.
- [x] 1.2 GREEN: remove `"tesina"` sentinel identifiers from `domain/evidence.py`, `collection.py`, `rules.py`, `sections.py`, `review.py`, `section_rendering.py`; keep `managed_by` gate semantics, atomic single-slice rename.
- [x] 1.3 RED: `tests/unit/infrastructure/test_filesystem_asset_repository.py` — asset-kind allow/reject scenarios (docx-only config still rejects non-docx).
- [x] 1.4 GREEN: `domain/ports/asset_repository.py` add `list_assets(directory, kind)` replacing `glob_docx`; `application/asset.py` validate by configurable asset-kind map.
- [x] 1.5 GREEN: `infrastructure/persistence/filesystem_asset_repository.py` implement kind-agnostic listing.
- [x] 1.6 GREEN: `pyproject.toml` — declare `docxcompose`, `filetype`, `opendataloader-pdf`.
- [x] 1.7 RED: `tests/unit/infrastructure/test_filesystem_source_repository.py` — git-helper failure surfaced (logged/raised), not swallowed.
- [x] 1.8 GREEN: `infrastructure/persistence/filesystem_source_repository.py` — replace silent `except Exception` with logged/re-raised error.
- [x] 1.9 Verify: `uv run pytest` green; grep confirms zero `"tesina"` literals in `src/docs/` domain layer swept by 1.1-1.2 (`evidence.py`, `collection.py`, `rules.py`, `sections.py`, `review.py`, `section_rendering.py`) plus the coupled `application/review.py` managed_by gate. `application/docx_assembly.py`'s `tesina-draft.docx`/`tesina-body.docx`, `application/pipeline.py`'s `_DRAFT_DOCX_NAME`, and `application/collection.py`'s `"tesina/context"` tag remain — they are explicitly owned by PR4/PR8 per design.md's file-changes table and are out of scope for this slice.
- [x] 1.10 Rollback: revert PR1 commit range; no data migration, no downstream dependents yet.

## PR2 — Repository Port Segregation (ISP)
Base: `main` (after PR1). Covers: document-pipeline (`Repository Port Segregation`).

- [x] 2.1 RED: `tests/unit/domain` — new tests asserting narrow ports (`RegistryRepository.active_id`, `DocumentRepository.read_document`, `TemplateRepository.load_template`) each usable independently.
- [x] 2.2 GREEN: split `domain/ports/document_repository.py` into `registry_repository.py`, `document_repository.py`, `template_repository.py`.
- [x] 2.3 GREEN: update `infrastructure/persistence/json_repository.py` to implement all three narrow ports on one adapter class.
- [x] 2.4 GREEN: update consumers (`application/documents.py`, `docx_assembly.py`, `context_pack.py`, others importing the old fat port) to depend on the narrow port they use. Verified via grep + CodeGraph blast-radius: the only actual importers of the fat port are `application/documents.py` and `application/context.py`; `docx_assembly.py`/`context_pack.py` never imported `DocumentRepository` (design.md's generic mention did not match current code). `context.py` needed zero changes — it already only calls `.exists()`, satisfied by the narrow `DocumentRepository`. `DocumentService` (documents.py) genuinely spans all three ports (registry+content+template), so it depends on a locally-defined `DocumentLifecycleRepository(RegistryRepository, DocumentRepository, TemplateRepository, Protocol)` composed type instead of reintroducing one fat protocol — zero call-site changes needed in `cli/_shared.py` or existing tests.
- [x] 2.5 Verify: `uv run pytest` green (existing `tests/integration/test_json_repository.py`, `test_document_service.py` unchanged behavior). Full suite: 790 passed, 7 skipped (787 baseline + 3 new port-segregation tests).
- [x] 2.6 Rollback: revert PR2; PR1 unaffected since it has no port dependency.

## PR3 — CLI Composition Root Split
Base: `main` (after PR2). Covers: document-pipeline (`CLI Composition Root Segregation`).

- [x] 3.1 RED: `tests/integration/test_cli_core.py` (+ existing `test_cli_collection/docx/template/doc/asset/context/section.py`) — assert each command still reachable post-split. DEVIATION: `test_cli_core.py` already existed pre-PR3 (written in an earlier characterization pass); it passed against the still-monolithic `main.py` and is not a valid RED anchor by itself. Added new `tests/integration/test_cli_composition_root.py` as the genuine RED for this slice — asserts (a) each `cli/commands/*_app.py` module exists and exposes the expected command set, and (b) the root app's flattened command tree is byte-identical to the pre-split snapshot. Confirmed RED (`ModuleNotFoundError: docs.cli.commands`) before 3.2.
- [x] 3.2 GREEN: split `cli/main.py` into `cli/commands/*_app.py` sub-Typer apps by concern (core, collection, docx, section, template, doc, asset, context). DEVIATION: no `ingest` placeholder module added — there are zero ingest commands in the current CLI to place, and PR5/PR6 own that port/adapter/wiring; adding an unmounted, untested stub file would be speculative dead code out of this slice's scope. `core`/`collection`/`docx`/`section` are new organizational modules (no group prefix existed for them) mounted via `app.add_typer(sub_app)` **without** a `name` — verified empirically that Typer/Click flattens an unnamed sub-Typer's commands onto the parent Group (no prefix), preserving the exact flat command surface. `template`/`doc`/`asset`/`context` keep their existing group names unchanged. Added a small `_ctx(ctx)` helper to `cli/_shared.py` (moved from `main.py`) so all command modules share one implementation without a `main.py` <-> `commands.*` import cycle.
- [x] 3.3 GREEN: delete root `main.py` (dead entrypoint). Verified via `codegraph explore` + repo-wide grep: no import of root `main.py` exists anywhere (pyproject.toml's `[project.scripts]` points at `docs.cli.main:main`, i.e. `src/docs/cli/main.py`, not the root file); root `main.py` only contained a placeholder `print("Hello from docs!")`.
- [x] 3.4 Verify: `uv run pytest` green (793 passed, 7 skipped — baseline 790 passed/7 skipped + 3 new composition-root tests); `python -m docs.cli.main --help` captured before and after the split and diffed byte-for-byte identical.
- [x] 3.5 Rollback: revert PR3; CLI entrypoint reassembled from sub-apps, no state impact. Confirmed: reverting restores the single monolithic `cli/main.py` + dead root `main.py`, no data/document state touched by this slice.

## PR4 — Renderer Registry + Data-Driven Stage Plan
Base: `main` (after PR3). Covers: document-render (all 4 requirements), document-pipeline (`Data-Driven, Format-Agnostic Stage Plan`).

- [x] 4.1 RED: `tests/unit/domain/test_pipeline.py` — `pipeline_stage_plan(stage_set, ...)` rejects unknown stage_set; same stage_set+format called twice returns identical ordered list (determinism). Confirmed RED (`TypeError: pipeline_stage_plan() takes 1 positional argument but 2 were given`) against the old one-arg signature before 4.3. Remediation round: added RED coverage that omitting `assemble` for `stage_set` in `assemble`/`all` raises `ValueError` instead of silently returning an empty/short stage list (fresh-context review WARNING).
- [x] 4.2 GREEN: create `domain/ports/document_renderer_port.py` (`DocumentRendererPort`: `output_format`, `stage_plan()`, `build()`).
- [x] 4.3 GREEN: rewrite `domain/pipeline.py:pipeline_stage_plan` to accept stage-name tuples from the resolved renderer — zero format literals. DEVIATION (narrower than design.md's full `prep, assemble, ingest` signature): only `assemble` became caller-supplied this slice; `_PREP_STAGES` stays a domain-module constant because it is format-agnostic (no DOCX/"tesina" identifiers), and the `ingest` stage_set does not exist until PR5. Adding an unused `ingest` parameter now would be speculative; the signature can grow additively when PR5 introduces the `ingest` stage_set. `pipeline_stage_plan` still only accepts `prep`/`assemble`/`all` — unchanged error message. Remediation round: `assemble=None` for `stage_set` in `assemble`/`all` now raises `ValueError` naming the missing argument, closing the silent-empty fallback.
- [x] 4.4 RED: `tests/integration/test_docx_assembly_service.py` extended — DOCX renderer resolves via registry, config-driven `tesina-draft.docx`/`tesina-body.docx` names become config values. Confirmed RED (`ImportError: cannot import name 'DocxRendererAdapter'`) before 4.5.
- [x] 4.5 GREEN: rename `application/docx_assembly.py` service to `DocxRendererAdapter` behind `DocumentRendererPort`; move hardcoded doc names into `config["output"]` (`draft_name`/`body_name`, defaulting to the prior `tesina-draft.docx`/`tesina-body.docx` values for backward compatibility). Remediation round (fresh-context review WARNING): added a behavior-level test (`test_build_produces_docx_at_configured_draft_and_body_names`) exercising real `build()` end-to-end with custom names, since the original private-helper-only test would not have caught `application/pipeline.py`'s audit/QA stages still hardcoding the default name.
- [x] 4.6 GREEN: `cli/_shared.py` — add `RENDERERS: dict[str, DocumentRendererPort]` map, resolve by `config["output"]["format"]` (default `"docx"`); raise clear error on unregistered format (no silent DOCX fallback). Implemented as `Deps.renderers` (registry built from each adapter's `output_format`) plus a module-level `resolve_renderer()` function and `Deps.resolve_renderer(config)` convenience wrapper. **Remediation round (fresh-context review CRITICAL)**: the registry was built but never consulted at runtime — `PipelineService` was fixed-wired to the DOCX renderer regardless of `config["output"]["format"]`, so an unregistered format (e.g. `"pdf"`) silently built DOCX instead of raising. Fixed by wiring `deps.resolve_renderer(resolved.config)` into `core_app.py`'s `pipeline` command and threading the resolved renderer through `PipelineService.run_pipeline(..., renderer=...)` / `_stage_callables(...)`, with the constructor-injected renderer kept only as a fallback default for callers that build `PipelineService` directly. New RED-confirmed test `tests/integration/test_cli_core.py::test_pipeline_unregistered_output_format_errors_cleanly_no_silent_docx`.
- [x] 4.7 RED / [x] 4.8 GREEN: new `tests/unit/application/test_renderer_registry.py` — register test-only fake `"txt"` renderer; pipeline resolves and builds via the fake with zero edits to `domain/pipeline.py`. DEVIATION: test and fixture were authored together in one file (common for test-only fakes) rather than as a separate failing-then-passing pair, since the underlying registry/domain plumbing it exercises was already TDD'd RED→GREEN in 4.1-4.6; this file's first run is itself the GREEN evidence that the extensibility contract holds. Remediation round: after the 4.6 CRITICAL fix, this wording is now genuinely satisfied through the real execution path too — `test_run_pipeline_assemble_threads_custom_draft_name_to_audit_and_qa` (added under 4.5's remediation) exercises a fake `DocumentRendererPort` through `PipelineService.run_pipeline` itself, not just the standalone registry function.
- [x] 4.9 Verify: `uv run pytest` green; DOCX regression suite (`test_docx_assembly_service.py`) unchanged; run pipeline twice, confirm identical stage-plan output (determinism). Initial pass: 812 passed, 7 skipped (baseline 799/7 + 13 new PR4 tests); stage-plan determinism confirmed both by unit test and an explicit double-run script (two independent `Deps` instances, identical 13-stage output). **Post-remediation pass**: 818 passed, 7 skipped (+6 tests: silent-`assemble`-fallback RED×2, unregistered-format-at-runtime RED×1, custom-draft-name-threading RED×2 across `run_pipeline`/`verify_all`, behavior-level custom-name `build()` test×1). DOCX regression suite still green and unchanged in intent — only additive assertions plus the mechanical `DocxAssemblyService`→`DocxRendererAdapter` rename.
- [x] 4.10 Rollback: revert PR4; registry is additive — reverting restores DOCX-only hardcoded path from PR3 baseline. Confirms: `DocxAssemblyService` fixed-wired, module-level `_ASSEMBLE_STAGES` in `domain/pipeline.py`; no data migration; `application/pipeline.py`'s `_DRAFT_DOCX_NAME` literal is untouched (owned by PR8) so reverting PR4 alone does not affect it.

## PR5 — opendataloader-pdf Verification + Ingest Detection/Routing Infra
Base: `main` (after PR4). Covers: document-ingest (`File-Type Detection`, `Type-Based Ingest Routing` routing only, `Empty inbox` scenario).

- [x] 5.1 Spike (non-code, blocking gate): verify `opendataloader-pdf` maturity/license/API stability — record findings in PR description; MUST complete and pass before 5.6/6.x lock the dependency in code. COMPLETED 2026-07-04 ahead of PR5 (user-ordered). VERDICT: GATE PASS with conditions. Findings: v2.4.7 (2026-05-27) pinned in pyproject/uv.lock and installed; Apache-2.0 license since v2.0 (MPL-2.0 before); actively maintained (repo updated 2026-06, 6.4k stars, 51 releases, Hancom-backed org with 2026 roadmap); stable Python `convert()` API wrapping a JVM CLI. Hard constraint: requires Java 11+ at runtime (OpenJDK Temurin 17 verified locally); each `convert()` call spawns a JVM, so ingest MUST batch all files into one call per inbox scan. Local-only processing by default — hybrid AI/OCR backends are opt-in flags and MUST stay off for determinism. Conditions binding 5.6/6.3/6.4: resolve Java via `ToolResolverPort` (clear error when absent, skip integration tests when unavailable, per existing pandoc/libreoffice pattern), local-only flags, empirical byte-determinism verified in 6.8. Full findings in Engram topic `sdd/universal-doc-harness/pdf-spike`; copy into PR5 description per this task's instruction.
- [x] 5.2 RED: `tests/unit/infrastructure/test_source_type_detector.py` — magic-byte detection for pdf/docx, extension fallback for md/txt, unknown-type returns `""`. Confirmed RED (`ModuleNotFoundError: No module named 'docs.infrastructure.ingest'`) before 5.3.
- [x] 5.3 GREEN: create `domain/ports/source_type_detector_port.py` (`SourceTypeDetectorPort.detect`); `infrastructure/ingest/filetype_detector_adapter.py` using `filetype` lib + extension fallback. Verified empirically (see Engram `sdd/universal-doc-harness/pr5-filetype-detection`): `filetype.guess` detects docx via zip-embedded `word/document.xml` signature and pdf via `%PDF` header even with a misleading extension; md/txt have no signature and correctly fall back to `.md`/`.txt` extension mapping.
- [x] 5.4 RED: `tests/unit/application/test_ingest_service.py` — router maps detected kind to handler stub; unsupported type recorded as `status: "unsupported"`, never raises; empty/missing inbox → zero files processed, no error. Confirmed RED (`ModuleNotFoundError: No module named 'docs.application.ingest'`) before 5.5.
- [x] 5.5 GREEN: create `domain/ports/source_ingest_port.py` (`SourceIngestPort.ingest`); `application/ingest.py` `IngestService` (detect -> route -> ingest, writes `inbox/_detection.json`). Scans skip `_`-prefixed inbox entries (reusing the `read_context_texts` convention) so the harness's own `_detection.json` report is never re-ingested as a source on the next scan.
- [x] 5.6 GREEN: idempotency — compute sha256 of source, derive `sha8`; skip re-ingest when `sections/ingested/<stem>-<sha8>.md` already recorded with matching hash. DEVIATION: the skip-if-output-exists check was implemented directly inside 5.5's `IngestService._ingest_one` (the deterministic `<stem>-<sha8>.md` path check *is* the "already recorded with matching hash" record — no separate manifest lookup needed), rather than being deferred to a standalone 5.6 change. `tests/unit/application/test_ingest_idempotency.py` adds RED-first-intent coverage with a `_HashNamingHandler` fixture that follows the real future-adapter naming convention (unlike 5.4's routing-only stub) and asserts: unchanged source is not re-invoked on a second run, changed content triggers a fresh ingest without touching/duplicating the prior output, and a partially-processed inbox only converts the newly added file. This file passed on first run (no implementation change required) — same test-locks-existing-contract precedent as PR4 tasks 4.7-4.8. FRESH-REVIEW REMEDIATION (WARNING 1): naming extended from `<stem>-<sha8>.md` to `<stem>-<kind>-<sha8>.md`. Reviewer proved a real collision — `readme.md` and `readme.txt` with byte-identical bodies share `stem` and `sha8`, so the old key silently marked the second file `"skipped"` with its handler NEVER invoked (observed RED: `txt calls: 0`, `readme.txt` reported `status: "skipped"`). Same stem + same kind cannot coexist in a flat inbox, so `(stem, kind, sha8)` is a unique identity; a rename across kinds correctly re-ingests since it is a different source. `test_same_stem_and_hash_but_different_kind_are_not_conflated` locks this in.
- [x] 5.7 Verify: `uv run pytest` green (839 passed, 7 skipped — baseline 818/7 + 21 new PR5 tests, including the two fresh-review remediation rounds). Routing/detection tests pass with stub adapters only, zero real pandoc/opendataloader-pdf calls made this slice. FRESH-REVIEW REMEDIATION (WARNING 2): `IngestService._ingest_one_safely` now wraps per-file detect/read/ingest in try/except — any exception is caught, reported as `status: "error"` with a deterministic `cause` field (`str(exc)`, no traceback, no timestamp), and the scan continues; `_detection.json` is always written for whatever was scanned. Reviewer-verified trigger reproduced and fixed: a raising handler/detector no longer aborts the whole batch (observed RED before the fix: uncaught `RuntimeError`/`FileNotFoundError` propagated out of `ingest_inbox`, and `_detection.json` was never written). Scoped narrowly — does NOT implement PR6 task 6.3's configured fail-fast for real per-type adapters.
- [x] 5.8 Rollback: revert PR5; confirmed via grep — zero references to `IngestService`/`SourceIngestPort`/`SourceTypeDetectorPort`/`FiletypeDetectorAdapter` in `application/pipeline.py`, `domain/pipeline.py`, or `cli/_shared.py`. No adapters wired into the pipeline or composition root yet, zero blast radius. Fresh-context review (post-fixes) verdict: MERGE-READY.

## PR6 — Ingest Per-Type Adapters
Base: `main` (after PR5, gated on 5.1 verification passing). Covers: document-ingest (`Type-Based Ingest Routing`, `Deterministic and Idempotent Ingest`, `Tool-Failure Reporting`).

- [x] 6.1 RED: `tests/integration/test_pandoc_ingest_adapter.py` — DOCX/ODT source -> pandoc `--extract-media` -> markdown + per-doc media dir; missing pandoc reports clear error via `ToolResolverPort`, no partial output. Confirmed RED (`ModuleNotFoundError: No module named 'docs.infrastructure.ingest.pandoc_ingest_adapter'`) before 6.2. DEVIATION (supporting infra, done first as its own RED/GREEN pair): added `tests/unit/domain/test_ingest_naming.py` + `domain/ingest_naming.py` (`sha256_hex`, `ingested_output_path`) extracting the `<stem>-<kind>-<sha8>.md` formula out of `application/ingest.py` into one canonical implementation, since every PR6 adapter must independently compute the identical final path to satisfy `IngestService`'s pre-existing skip-check (the `SourceIngestPort.ingest(src, out_dir)` signature carries no `kind`/`sha8` parameter, by design.md's own contract). Also added `tests/unit/application/test_ingest_service.py::test_handler_failure_preserves_detected_kind_in_error_entry` (confirmed RED: `AssertionError: assert 'unknown' == 'pdf'`) closing carry-forward (b) below — `_ingest_one_safely` now runs detection outside the try/except so a resolved kind survives into `status: "error"` entries.
- [x] 6.2 GREEN: `infrastructure/ingest/pandoc_ingest_adapter.py` implementing `SourceIngestPort` for docx/odt. `kind` is re-derived from the source's own extension (`src.suffix`) since the port signature carries no kind parameter; pandoc runs inside a scratch dir under `out_dir` (temp-then-atomic-rename via new `infrastructure/ingest/atomic_ingest_write.py`) so a missing pandoc or a failed conversion never leaves a partial file at the final path. 4/4 tests green (docx+media, odt, missing-pandoc, corrupt-docx-leaves-no-partial-output). FRESH-REVIEW REMEDIATION (FINDING 2): the two-step finalize (media dir moved, then `.md` moved) could leave an orphaned `<stem>-<kind>-<sha8>_media/` directory if the process died between the two `atomic_finalize` calls — `IngestService`'s skip-check only looks at the `.md`, so a retry would re-run pandoc and then fail `os.replace`-ing onto that stale non-empty directory instead of converging. Confirmed RED first with `test_retry_after_orphaned_media_dir_converges_to_complete_output` (pre-existing stale media dir + missing `.md`, `TypeError`/failure before the fix). Fix: since `final_md`'s absence is already guaranteed by the skip-check before `ingest()` is ever called, any media dir already present at that exact content-addressed path is provably an orphan from an earlier failed attempt, so the adapter now `shutil.rmtree`s it before finalizing the fresh one — the retry always converges to a complete, correct `.md` + media dir pair.
- [x] 6.3 RED: `tests/integration/test_opendataloader_pdf_adapter.py` — PDF -> markdown; conversion failure reports cause per-file and applies configured fail-fast. Confirmed RED (`ModuleNotFoundError: No module named 'docs.infrastructure.ingest.opendataloader_pdf_adapter'`) before 6.4. NOTE (carried from PR5 fresh-review round 2, binding on all PR6 adapters): (a) adapters MUST write temp-then-atomic-rename or otherwise guarantee that a handler failing mid-write leaves no partial file at `sections/ingested/<stem>-<kind>-<sha8>.md` — otherwise IngestService's skip check permanently accepts the corrupt output as "skipped" on the next run (proven empirically against PR5's routing infra); SATISFIED via `atomic_ingest_write.scratch_dir`/`atomic_finalize` in all three adapters, tested explicitly (`test_conversion_failure_leaves_no_partial_output_for_failed_file`, `test_missing_pandoc_reports_clear_error_and_leaves_no_partial_output`, md/txt has no failure mode to test since it is pure-Python). (b) restructure error entries so a kind already resolved by the detector survives into the `status: "error"` entry instead of the current unconditional `"kind": "unknown"` — SATISFIED, see 6.1's `test_handler_failure_preserves_detected_kind_in_error_entry`.
- [x] 6.4 GREEN: `infrastructure/ingest/opendataloader_pdf_adapter.py` implementing `SourceIngestPort` for pdf (5.1 sign-off already recorded GATE PASS with conditions). Verified empirically against installed `opendataloader-pdf==2.4.7` + OpenJDK 17: `convert()` writes `<stem>.md` per input into `output_dir`; a batch call with one corrupt PDF among good ones still writes output for the good files even though the overall subprocess exits non-zero (confirmed via a real 3-file experiment before writing the adapter). DEVIATION: `opendataloader_pdf`'s bundled `runner.run_jar` hardcodes the literal `"java"` command (no injectable executable path), so `ToolResolverPort.resolve_java` (new port method + `resolve_java_executable`, mirroring `resolve_pandoc_executable`'s PATH-then-config-fallback shape) is honored via a scoped `_java_on_path` context manager that temporarily prepends the resolved java's directory to `PATH` for the duration of the call, restored immediately after — otherwise a configured `java_bin` off PATH would be silently ignored by the underlying library. DEVIATION (batching, satisfies "batch ALL pdf files into ONE convert() call per inbox scan" from the 5.1 spike): since `SourceIngestPort.ingest(src, out_dir)` is called once per file by `IngestService`'s existing per-file loop (unchanged, out of PR6 scope to restructure), the adapter itself looks ahead at sibling `.pdf` files still pending conversion in the same inbox directory on its first `ingest()` call of a scan, batches them all into one `convert()` call, and caches per-file results so later `ingest()` calls for those siblings are free — locked in by `test_batches_all_pending_pdfs_in_one_convert_call` (monkeypatched spy asserting exactly 1 call for 3 files) and `test_one_corrupt_pdf_in_a_batch_does_not_abort_its_siblings`. 8/8 tests green (2 resolver unit tests inline in the same file, missing-java, real pdf-to-markdown, batching, corrupt-sibling-isolation, no-partial-output — all real-tool tests `skipif not shutil.which("java")`). FRESH-REVIEW SUGGESTION (deferred by orchestrator decision): a reviewer suggested extracting the Java-resolution logic (`resolve_java_executable`, `_java_on_path`) out of this file into a shared `tool_resolution` module alongside `resolve_pandoc_executable`. Not implemented this session — deferred, no action taken.
  - ADDITIVE NOTE (tech-debt closeout, pre-PR7, item D5): this deferral is now resolved. `resolve_java_executable` and `_java_on_path` (renamed `java_on_path`) moved to `infrastructure/tools/java_resolution.py`; both `tool_resolver_adapter.py` and `opendataloader_pdf_adapter.py` import from the new shared location. No import of the old location survives (verified via grep); all existing tests, including the real-tool opendataloader integration tests, stay green.
- [x] 6.5 RED: `tests/unit/infrastructure/test_md_normalize_adapter.py` — md/txt frontmatter normalization, reuse `split_frontmatter`. Confirmed RED (`ModuleNotFoundError: No module named 'docs.infrastructure.ingest.md_normalize_adapter'`) before 6.6.
- [x] 6.6 GREEN: `infrastructure/ingest/md_normalize_adapter.py` implementing `SourceIngestPort` for md/txt. Reuses `domain/markdown_text.split_frontmatter` unchanged; JSON frontmatter is re-serialized with sorted keys (canonical, deterministic on re-run), body and any unparseable/unclosed frontmatter pass through byte-for-byte (matching `split_frontmatter`'s own fallback). 5/5 tests green.
- [x] 6.7 GREEN: wire all three adapters into `IngestService` routing table (`application/ingest.py`) and `cli/_shared.py` registration. `Deps.__init__` now builds one `PandocIngestAdapter` registered under both `"docx"`/`"odt"`, one `OpendataloaderPdfAdapter` under `"pdf"`, and one `MdNormalizeAdapter` under both `"md"`/`"txt"`, exposed as `self.ingest: IngestService`. New `tests/integration/test_deps_ingest_wiring.py` (5/5 green) locks in the composition-root wiring itself (handler-set membership + same-instance sharing for docx/odt and md/txt) since no CLI `ingest` command exists yet to exercise it end-to-end (that command wiring is PR8's `application/pipeline.py` stage-callable work per this file's own PR8 section — out of PR6 scope). FRESH-REVIEW REMEDIATION DEVIATION (FINDING 1, additive port change): `SourceIngestPort.ingest(src, out_dir)` grew a required third `kind: str` parameter — `IngestService._ingest_one` now passes its already-resolved `kind` straight through to `handler.ingest(src, ingested_dir, kind)` instead of each adapter re-deriving kind from `src.suffix`. Reproduced and confirmed RED first: a real DOCX renamed `report.doc` was correctly detected as `"docx"` by `FiletypeDetectorAdapter` and routed to `PandocIngestAdapter`, but the adapter passed no `-f` reader to pandoc and re-derived `kind="doc"` from the filename, so pandoc failed with `Unknown input format 'doc'` (exit code) and the file could never be ingested; the output-naming split (`-doc-` vs `-docx-`) also permanently broke the idempotency skip-check identity. Fix: all three adapters (`PandocIngestAdapter`, `OpendataloaderPdfAdapter`, `MdNormalizeAdapter`) now use the passed `kind` for output naming; `PandocIngestAdapter` additionally passes an explicit `-f <reader>` (mapped from `kind`) so pandoc never infers the input format from the source's extension. `OpendataloaderPdfAdapter`'s underlying `opendataloader-pdf` CLI independently validates its input by file EXTENSION (`'paper.txt' is not a PDF file`), so the adapter now stages each candidate into a `.{kind}`-suffixed scratch copy before calling `convert()`, decoupling the tool's own extension check from the source's real filename. New tests: `test_misleading_extension_docx_is_ingested_via_detected_kind_not_suffix` (end-to-end through `IngestService`+real pandoc), plus unit-level naming assertions in the pdf and md/txt adapter test files; all existing adapter tests and fakes updated to the 3-arg signature. This additive change supersedes the 3-arg-carries-no-kind statement in design.md's `SourceIngestPort` sketch (design.md left unedited per its frozen/additive-only convention — the port's actual, tested contract is now `ingest(src, out_dir, kind)`).
- [x] 6.8 Test — Determinism: `tests/integration/test_ingest_determinism.py` — run ingest twice over the same inbox fixture, assert byte-identical `.md` output and unchanged behavior on partially-processed inbox (no duplication/corruption). Exercises the real composition-root wiring end to end (not stubs): md/txt determinism always runs (no external tool), docx/pdf determinism tests `skipif` when pandoc/java are unavailable. 5/5 tests green, including empty-inbox and partial-inbox-not-corrupted scenarios.
- [x] 6.9 Verify: `uv run pytest` green including new integration adapters (pandoc/opendataloader-pdf test-doubled or skipped if tool unavailable in CI, per existing `ToolResolverPort` pattern). Full suite: 869 passed, 7 skipped (baseline 839/7 + 30 new PR6 tests: 2 naming + 1 error-kind + 4 pandoc + 8 pdf + 5 md-normalize + 5 Deps-wiring + 5 determinism). Both pandoc and Java/opendataloader-pdf are available in this dev environment, so all real-tool tests ran for real (none skipped); the `skipif`/skip-when-unavailable guards are in place and match the existing pandoc/libreoffice pattern for CI environments lacking those tools. FRESH-REVIEW REMEDIATION RE-VERIFY: after the FINDING 1 / FINDING 2 fixes above, `uv run pytest` is green at 873 passed, 7 skipped (869/7 baseline + 4 new remediation tests: 1 misleading-extension end-to-end, 1 orphaned-media-dir retry, 2 passed-kind-naming unit checks for pdf/md).
- [x] 6.10 Rollback: revert PR6; PR5 routing infra still functions with adapters absent (falls back to `"unsupported"` reporting). Confirmed via grep: zero references to `PandocIngestAdapter`/`OpendataloaderPdfAdapter`/`MdNormalizeAdapter`/`resolve_java` outside `infrastructure/ingest/`, `tool_resolver_adapter.py`/`tool_resolver_port.py`, and `cli/_shared.py`'s wiring block — reverting PR6's commits cleanly drops the handler registrations and adapter files with no other call site to unwind. The `application/ingest.py` kind-preservation-on-error fix and naming-helper extraction are the only PR5-file changes; both are behavior-preserving refactors (same computed values, same public `ingest_inbox` contract, verified by the unchanged PR5 tests still passing), so reverting them is also safe in isolation.

**Line-budget risk flagged for orchestrator review**: `git diff main --stat` reports 1,027 changed lines (852 insertions + 12 deletions across the first four commits, plus 163 insertions for the determinism test = 1,015 insertions + 12 deletions, 16 files) — well above the ~340-line forecast and the 400-line chained-PR budget. The overage is concentrated in the two real-external-tool integration test files (`test_opendataloader_pdf_adapter.py` 199 lines, `test_pandoc_ingest_adapter.py` 121 lines, `test_ingest_determinism.py` 163 lines) needed to prove the binding atomic-write/batching/fail-fast constraints empirically against real pandoc/Java rather than mocks. This PR was implemented as already-committed, reviewable work units (one per adapter pair) on `feat/pr6-ingest-adapters`; splitting further into separate chained PRs was not done in this session and is left to the orchestrator's Review Workload Guard / delivery-strategy decision before merge.

## PR7 — Context Files + Progressive-Disclosure Index
Base: `main` (after PR6). Covers: context-curation (all 4 requirements).

- [x] 7.1 RED: `tests/unit/application/test_context_files.py` — skeleton generation produces deterministic headings/instructions + explicit empty `AGENT-FILL` block per concern (keywords/tone/structure/writing-style/formatting-rules). Confirmed RED (`ModuleNotFoundError: No module named 'docs.application.context_files'`) before 7.2.
- [x] 7.2 GREEN: create `application/context_files.py` — pure functions building skeletons from ingested markdown. `build_context_file_skeleton(concern, ingested_texts)`: `keywords` reuses `domain/markdown_text.keyword_set`; the other four concerns reuse `extract_markdown_headings`+`dedupe_strings` for the deterministic "Extracted (auto)" section — mechanical extraction only, no cognitive judgement, per design's harness/agent boundary.
  - ADDITIVE NOTE (fresh-context review remediation, pre-PR8, marker naming): the `AGENT-FILL` marker template was renamed from `AGENT-FILL:{concern}-content` to `AGENT-FILL:curated-{concern}` to match design.md's illustrative example literally (`AGENT-FILL:curated-keywords`). design.md's snippet only shows the `keywords` concern; the same `curated-<concern>` prefix pattern is extrapolated to the other four (`curated-tone`, `curated-structure`, `curated-writing-style`, `curated-formatting-rules`). Verified via grep before renaming: zero references to the old marker string outside this module and its own test file (module was unwired, no persisted context files exist anywhere), so the rename is safe with no migration needed. `tests/unit/application/test_context_files.py`'s `_fill_marker` helper updated to match; RED-confirmed first (5 tests failed against the renamed helper before the source rename, including the two malformed/CRLF tests below).
- [x] 7.3 RED: test — re-running skeleton generation over a file with existing `AGENT-FILL` content does not overwrite/discard it (idempotent merge). Covered together with 7.1 in the same RED-first file (regex-based `AGENT-FILL:{concern}-content START/END` markers did not exist yet, so the merge assertions failed on the same `ModuleNotFoundError` before 7.4).
- [x] 7.4 GREEN: implement idempotent merge preserving `AGENT-FILL` blocks on regeneration. `merge_context_file(concern, new_skeleton, existing_text)` returns the fresh skeleton unchanged when there is no prior file or the prior `AGENT-FILL` block is still empty; otherwise it splices the prior non-empty content into the freshly regenerated skeleton (so sibling mechanical content — extracted terms, instructions — still refreshes on every run).
  - ADDITIVE NOTE (fresh-context review remediation, pre-PR8, three findings fixed on this branch): (1) a malformed/unterminated `AGENT-FILL` block (START marker present, no matching END marker) previously matched the regex search's `if not match` branch and silently fell back to the fresh skeleton, discarding whatever the agent had written with no error. Reviewer repro confirmed live: `merge_context_file("tone", fresh, "...<!-- AGENT-FILL:...-content START -->\nUnterminated content\n")` returned the empty fresh skeleton. DECISION: fail loudly — `merge_context_file` now raises `ValueError` when a START marker is found without its matching END marker, rather than guessing at recovery (which risks capturing unrelated trailing file content as "agent content") or silently discarding. This is consistent with `build_context_file_skeleton`'s existing convention of raising `ValueError` on an unknown concern instead of silently producing a malformed file, and satisfies the spec's context-curation "Skeleton regeneration preserves agent content" requirement (`specs/context-curation/spec.md`) that agent-authored content MUST NOT be silently discarded — an explicit, deterministic failure is compatible with "MUST NOT discard" where a silent fallback is not. RED-confirmed first (`Failed: DID NOT RAISE ValueError`) via `test_malformed_unterminated_agent_fill_block_raises_instead_of_silently_discarding`. (2) CRLF line endings in `existing_text` broke the marker regex (built with a literal `\n`), silently discarding agent content for any input using `\r\n`/`\r`. All real callers use `Path.read_text()` (which normalizes line endings), so this was latent, not yet reachable — fixed anyway since the function is public and pure: `existing_text` is now normalized to `\n` on input before matching (output stays LF-only, consistent with the module's determinism invariant). RED-confirmed first (`AssertionError: assert 'Keep a formal, academic tone.' in '...'` — content silently lost) via `test_merge_handles_crlf_line_endings_without_losing_agent_content`. (3) see 7.2's additive note for the `AGENT-FILL:curated-<concern>` marker-naming rename, implemented in the same remediation pass since all three fixes touch the same regex/marker helpers. Full suite after remediation: 895 passed, 7 skipped (893 baseline + 2 new tests).
- [x] 7.5 RED: `tests/unit/application/test_context_index.py` — exactly one `index.md` generated, 3-level structure (overview, per-file summary with links, references pointer), reuses `read_context_texts` skip rules (`index.md`, `_`-prefixed). SELF-CORRECTION: `build_context_index` was initially drafted alongside 7.2's implementation (ahead of this dedicated RED task), which would have made this test pass without ever failing first — a strict-TDD violation. Corrected before proceeding: removed `build_context_index` from `context_files.py`, re-ran this test file, confirmed genuine RED (`ImportError: cannot import name 'build_context_index' from 'docs.application.context_files'`), then restored the implementation as 7.6's dedicated GREEN step.
- [x] 7.6 GREEN: implement index builder in `application/context_files.py`. `build_context_index(context_files)` returns exactly one Markdown string (`## Overview` / `## Files` / `## References`), sorting and filtering entries via `_is_indexable` (mirrors `FilesystemSourceRepository.read_context_texts`'s skip rule: excludes the stem `"index"` and any `_`-prefixed stem). DEVIATION (scope clarification, additive): design.md's phrase "replacing JSON-only `context/index.json` with agent-readable markdown index" is satisfied *within this new context-curation module only* — this module never writes a JSON companion, only `index.md`. It does NOT touch the pre-existing, unrelated `JsonContextRepository.regenerate_index` (the Topic/Q&A context-schema subsystem in `application/context.py` / `infrastructure/persistence/json_context_repository.py`), which already independently writes both `context/index.json` and a different-shaped `context/index.md` (a topic-status table). That subsystem is out of the context-curation spec's scope (the spec's 4 requirements never mention it, and no MODIFIED delta for `context_repository` exists), so migrating/deleting it here would be undocumented scope creep and would break existing `context status/elicit/ingest/set/rm` CLI commands with zero spec coverage for the change. Verified via codegraph + grep: zero references to `context_files`/`build_context_index`/`build_context_files` in `application/pipeline.py`, `domain/pipeline.py`, or `cli/_shared.py` — this module stays fully additive/unwired, consistent with PR5/PR6 precedent; wiring (including any future call-site consolidation) is left for PR8 or a later slice if the orchestrator decides the two systems should converge.
- [x] 7.7 Test — Determinism: same ingested sources + config run twice -> identical file set and skeleton structure per file (`tests/unit/application/test_context_files.py::test_same_inputs_same_output`, plus `test_context_index.py::test_index_generation_is_deterministic_across_runs` for the index).
- [x] 7.8 Test — No auto-invoke: assert context-curation completion triggers no agent process call. `test_no_agent_process_invoked_on_completion` monkeypatches `subprocess.run` to raise if called, then exercises `build_context_files` end-to-end — passes because the module performs zero I/O and zero subprocess/agent invocation by construction.
- [x] 7.9 Verify: `uv run pytest` green. Full suite: 893 passed, 7 skipped (877 baseline + 16 new PR7 tests: 11 in `test_context_files.py`, 5 in `test_context_index.py`).
- [x] 7.10 Rollback: revert PR7; ingest pipeline (PR5/PR6) unaffected since context stage is additive. Confirmed via grep: zero references to `context_files.py`'s public functions in `application/pipeline.py`, `domain/pipeline.py`, or `cli/_shared.py` — reverting this slice's two new source files and two new test files cleanly drops the feature with no other call site to unwind.

## PR8 — Pipeline Wiring + App-Layer Test/Debt Closeout
Base: `main` (after PR7). Covers: document-pipeline (`Application-Layer Test Coverage and Index De-duplication`), proposal success criterion (full pipeline reproducibility).

- [ ] 8.1 GREEN: `application/pipeline.py` — add `ingest` and `build-context-files`/`build-context-index` stage callables to the `ingest` stage_set; drop `_DRAFT_DOCX_NAME` literal (superseded by PR4 config).
  - ADDITIVE NOTE (tech-debt closeout, pre-PR7, item D1): the `_DRAFT_DOCX_NAME` literal drop was completed early, in the debt-closeout slice ahead of PR7. `pipeline.py` and `application/docx_assembly.py`'s `DocxRendererAdapter` both now resolve the draft/body default names from one shared module, `application/output_names.py` (`resolve_draft_docx_name`/`resolve_body_docx_name`), instead of each declaring its own copy of the `"tesina-draft.docx"` literal. RED test: `tests/integration/test_pipeline_service.py::test_pipeline_and_renderer_resolve_draft_name_from_one_shared_default` (monkeypatches the one shared default and asserts both `PipelineService._resolve_draft_docx_name` and `DocxRendererAdapter._draft_docx_name` follow it). The remaining half of this task (ingest / build-context-files / build-context-index stage-callable wiring) is still open for PR8.
  - NOTE (carried from PR7 fresh-review remediation, binding on this task's `build-context-index` wiring): `application/context_files.py:build_context_index` and the pre-existing, unrelated `JsonContextRepository.regenerate_index` (`infrastructure/persistence/json_context_repository.py`, the Topic/Q&A context-schema subsystem) both target the same on-disk path, `context/index.md`, with two incompatible formats — the new 3-level progressive-disclosure index (overview/files/references) versus the existing topic-status table. Harmless today because `build_context_index` is unwired (7.6 confirmed via grep: zero call sites in `application/pipeline.py`, `domain/pipeline.py`, or `cli/_shared.py`), but this task's wiring MUST resolve the collision before both writers can run against the same document — either consolidate into one writer/format or namespace the two outputs (e.g. distinct filenames) so neither silently overwrites the other's most recent write.
- [ ] 8.2 RED: identify application services lacking unit tests (pipeline, asset, ingest, context) via `uv run pytest --collect-only`; write missing unit tests per service.
- [ ] 8.3 GREEN: close test gaps found in 8.2 (one passing unit test per uncovered application service).
- [ ] 8.4 RED: `tests/unit/domain/test_sections.py` (or shared module) — assert exactly one `_sections_index` implementation is exercised by all former call sites.
- [ ] 8.5 GREEN: de-duplicate `_sections_index` into a single shared implementation; update all call sites.
- [ ] 8.6 Test — Full-pipeline determinism: `tests/integration/test_pipeline_service.py` extension — run `ingest` -> `build-context-files` -> `assemble` twice on the same fixture inbox/config; assert identical outputs end-to-end (proposal success criterion).
- [ ] 8.7 Verify: `uv run pytest` full suite green; grep confirms no remaining `"tesina"` or dead `main.py`.
- [ ] 8.8 Rollback: revert PR8; wiring is additive on top of PR4-PR7, DOCX-only path still reachable via existing stage_set `assemble`.

---

## Notes

- Determinism verification tasks: 4.9 (stage plan), 6.8 (ingest byte-identical), 7.7 (context skeleton set), 8.6 (full pipeline, double-run).
- Extensibility proof: 4.7-4.8 (fake `"txt"` renderer, no `domain/pipeline.py` edits).
- Gate: 5.1 (opendataloader-pdf verification) MUST pass before 5.6 (hash/lock groundwork) and PR6 tasks 6.3-6.4 (real PDF adapter) proceed.
- Each PR is independently revertible; stacked-to-main means every merge must leave `main` green — do not merge a slice with failing `uv run pytest`.

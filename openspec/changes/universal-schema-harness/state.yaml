change: universal-schema-harness
artifact_store: openspec
created: 2026-07-06
updated: 2026-07-06

phases:
  explore:
    status: done
    artifact: explore.md
  propose:
    status: done
    artifact: proposal.md
  spec:
    status: done
    artifact: specs/ (document-template new; document-pipeline, document-ingest, asset-management deltas)
  design:
    status: done
    artifact: design.md
  tasks:
    status: done
    artifact: tasks.md
  apply:
    status: done
    note: >
      ALL 6 BATCHES COMPLETE - implementation feature-complete.
      Fronts A-G shipped across PRs #12 (A policy de-hardcode), #13 (B media
      cleanup + doctor), #14 (C recursive ingest), #15 (D+E roles +
      near-duplicates), #16 (F verbatim assets + figure catalog), and the
      final Front G (template init/validate + gap report) on branch
      feat/usch-g-template-lifecycle.
      Verify caught a CRITICAL in 4 of 6 slices, every one an inert-feature
      or vacuous-test class: normative_source silent loss (A), media
      file-level data loss (B), accent normalization masked by a
      byte-identical-input test (D+E), placement confirmation never reaching
      structure_parts (F), and gap report self-satisfied by its own PENDIENTE
      scaffold text (G, root-caused in the shared requirement_present -
      review-section/review-document were silently passing unwritten sections
      too). All fixed test-first with non-vacuous proofs.
      Suite: 1089 passing. Open follow-ups (Phase 13): 13.1 no-literal guard
      scope, 13.2/13.3 figure assembly consumer + image heuristic scope,
      NEW-SUGGESTION-3 empty nested subdir sweep.
      Remaining: Phase 12 final acceptance against the user's real drop, then
      archive.
  verify:
    status: pending
  archive:
    status: pending

next_recommended:
  - resolve-delivery-decision   # Review Workload Guard fired: chained PRs recommended, chain strategy pending
  - sdd-apply

capabilities:
  new:
    - document-template
  modified:
    - document-pipeline
    - document-ingest
    - asset-management
  unchanged:
    - context-curation
    - document-render

delivery_strategy: ask-on-risk
notes: >
  Multi-front change; expect chained PRs (policy de-hardcoding; recursive
  ingest + roles/duplicates; assets/figures; template init/validate + gap
  report). PR boundaries deferred to sdd-tasks.

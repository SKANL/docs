change: universal-schema-harness
artifact_store: openspec
created: 2026-07-06
updated: 2026-07-06

phases:
  explore:
    status: done
    artifact: explore.md
  propose:
    status: done
    artifact: proposal.md
  spec:
    status: done
    artifact: specs/ (document-template new; document-pipeline, document-ingest, asset-management deltas)
  design:
    status: done
    artifact: design.md
  tasks:
    status: done
    artifact: tasks.md
  apply:
    status: in-progress
    note: >
      batch 5/6 COMPLETE: Front F (verbatim asset pre-ingest routing,
      _placement-queue.json, confirmed-placement wiring into resolved
      structure, deterministic figure catalog) on branch
      feat/usch-f-assets-figures (7 commits), verify pass 1 needs-fixes
      (CRITICAL: placement confirmation inert - manifest half only, never
      reached structure_parts) -> fixed (_apply_confirmed_placements in
      resolve_context) + orchestrator self-verified the small fix (RED test
      against real structure_parts consumer; auto-confirm invariant holds:
      structure_part only set when confirmed_placement truthy). PRs #12 (A)
      #13 (B) #14 (C) #15 (D+E) MERGED. Next+last: Front G (template
      lifecycle + gap report). Open follow-ups: 13.1 guard scope, 13.2/13.3
      figure assembly consumer + heuristic scope, NEW-SUGGESTION-3.
  verify:
    status: pending
  archive:
    status: pending

next_recommended:
  - resolve-delivery-decision   # Review Workload Guard fired: chained PRs recommended, chain strategy pending
  - sdd-apply

capabilities:
  new:
    - document-template
  modified:
    - document-pipeline
    - document-ingest
    - asset-management
  unchanged:
    - context-curation
    - document-render

delivery_strategy: ask-on-risk
notes: >
  Multi-front change; expect chained PRs (policy de-hardcoding; recursive
  ingest + roles/duplicates; assets/figures; template init/validate + gap
  report). PR boundaries deferred to sdd-tasks.

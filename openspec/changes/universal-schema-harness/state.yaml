change: universal-schema-harness
artifact_store: openspec
created: 2026-07-06
updated: 2026-07-06

phases:
  explore:
    status: done
    artifact: explore.md
  propose:
    status: done
    artifact: proposal.md
  spec:
    status: done
    artifact: specs/ (document-template new; document-pipeline, document-ingest, asset-management deltas)
  design:
    status: done
    artifact: design.md
  tasks:
    status: done
    artifact: tasks.md
  apply:
    status: in-progress
    note: >
      batch 3/7 COMPLETE: Front C (recursive ingest, provenance,
      honest status vocabulary, IngestArtifactWriter port) on branch
      feat/usch-c-recursive-ingest (4 commits), verify pass 1: ready-for-pr
      (first slice to pass without a fix cycle). PR #12 (A) and PR #13 (B)
      MERGED. Next: Front D (roles/duplicates), folding in PR3's
      non-blocking findings (harness_artifact ignored-reason,
      inbox/assets boundary pinning test, sort-order doc note).
      Open follow-ups: task 13.1 (guard scope), NEW-SUGGESTION-3
      (empty nested subdir sweep).
  verify:
    status: pending
  archive:
    status: pending

next_recommended:
  - resolve-delivery-decision   # Review Workload Guard fired: chained PRs recommended, chain strategy pending
  - sdd-apply

capabilities:
  new:
    - document-template
  modified:
    - document-pipeline
    - document-ingest
    - asset-management
  unchanged:
    - context-curation
    - document-render

delivery_strategy: ask-on-risk
notes: >
  Multi-front change; expect chained PRs (policy de-hardcoding; recursive
  ingest + roles/duplicates; assets/figures; template init/validate + gap
  report). PR boundaries deferred to sdd-tasks.

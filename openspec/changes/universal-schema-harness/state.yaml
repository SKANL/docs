change: universal-schema-harness
artifact_store: openspec
created: 2026-07-06
updated: 2026-07-06

phases:
  explore:
    status: done
    artifact: explore.md
  propose:
    status: done
    artifact: proposal.md
  spec:
    status: done
    artifact: specs/ (document-template new; document-pipeline, document-ingest, asset-management deltas)
  design:
    status: done
    artifact: design.md
  tasks:
    status: done
    artifact: tasks.md
  apply:
    status: in-progress
    note: >
      batch 4/7 COMPLETE: Fronts D+E (source-role classification with
      confirmation queue, 5-word-shingle Jaccard near-duplicate detection
      with accent/markdown normalization) on branch
      feat/usch-d-roles-duplicates (13 commits), verify passes 1+2
      (needs-fixes: accent-normalization CRITICAL masked by a vacuous
      test -> ready-for-pr with non-vacuous-test proof). PRs #12 (A),
      #13 (B), #14 (C) MERGED. Next: Front F (verbatim assets + figure
      catalog). Open follow-ups: task 13.1 (guard scope), NEW-SUGGESTION-3
      (empty nested subdir sweep), role_status has no cross-service
      consumer yet (deferred to G/gap-report wiring).
  verify:
    status: pending
  archive:
    status: pending

next_recommended:
  - resolve-delivery-decision   # Review Workload Guard fired: chained PRs recommended, chain strategy pending
  - sdd-apply

capabilities:
  new:
    - document-template
  modified:
    - document-pipeline
    - document-ingest
    - asset-management
  unchanged:
    - context-curation
    - document-render

delivery_strategy: ask-on-risk
notes: >
  Multi-front change; expect chained PRs (policy de-hardcoding; recursive
  ingest + roles/duplicates; assets/figures; template init/validate + gap
  report). PR boundaries deferred to sdd-tasks.

change: universal-schema-harness
artifact_store: openspec
created: 2026-07-06
updated: 2026-07-06

phases:
  explore:
    status: done
    artifact: explore.md
  propose:
    status: done
    artifact: proposal.md
  spec:
    status: done
    artifact: specs/ (document-template new; document-pipeline, document-ingest, asset-management deltas)
  design:
    status: done
    artifact: design.md
  tasks:
    status: done
    artifact: tasks.md
  apply:
    status: in-progress
    note: >
      batch 2/7 COMPLETE: Front B (orphan _media cleanup with
      refuse-on-unexpected-content hardening, doctor.py de-hardcode) on
      branch feat/usch-b-bootstrap-media (8 commits), verify passes 1+2
      (needs-fixes -> ready-for-pr). PR #12 (batch 1, Front A) MERGED at
      d7ad6b5. Next batch: Front C (recursive ingest) after PR2 merges.
      Open follow-ups: task 13.1 (no-literal guard scope), NEW-SUGGESTION-3
      (empty nested subdir in orphan media dir silently swept).
  verify:
    status: pending
  archive:
    status: pending

next_recommended:
  - resolve-delivery-decision   # Review Workload Guard fired: chained PRs recommended, chain strategy pending
  - sdd-apply

capabilities:
  new:
    - document-template
  modified:
    - document-pipeline
    - document-ingest
    - asset-management
  unchanged:
    - context-curation
    - document-render

delivery_strategy: ask-on-risk
notes: >
  Multi-front change; expect chained PRs (policy de-hardcoding; recursive
  ingest + roles/duplicates; assets/figures; template init/validate + gap
  report). PR boundaries deferred to sdd-tasks.

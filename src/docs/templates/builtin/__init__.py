"""Built-in template JSONs, shipped as package data.

Canonical home for templates previously only available under
`tests/fixtures/templates/` (design.md item C). Read via
`importlib.resources.files("docs.templates.builtin")` — never a hardcoded
filesystem path, so this resolves from an installed wheel with no repo
checkout.
"""
